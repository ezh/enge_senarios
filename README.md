# Engineering Scenario Editor

# Install/Deploy

This application runs on CouchDB, get that installed first.

~~~~~ shell
$ npm install
$ webpack # install webpack if necessary: npm install -g webpack
$ couchapp push couchapp enge_scenarios
~~~~~
